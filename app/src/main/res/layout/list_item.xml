<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip" 
   >

    <TextView
        android:id="@+id/addressTextView"
        android:layout_width="fill_parent"
        android:layout_height="26dip"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"    
        android:ellipsize="marquee"
        android:singleLine="true"
        android:layout_toLeftOf="@+id/typeTextView"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/nameTextView"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/addressTextView"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toLeftOf="@+id/pairTextView"
        android:gravity="center_vertical"
        android:textSize="16sp" />

  <TextView
      android:id="@+id/pairTextView"
      style="@style/RelativeLayoutStyle"
      android:layout_width="64dp"
      android:layout_height="wrap_content"
      android:layout_above="@+id/typeTextView"
      android:layout_alignParentRight="true"
      android:layout_alignTop="@+id/nameTextView"
      android:layout_alignWithParentIfMissing="true"
      android:layout_marginRight="5dp"
      android:gravity="center"
      android:text="@string/ada_prompt_info_unpair_state"
      android:textSize="12sp" />
  
   <TextView
        android:id="@+id/typeTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"  
        android:layout_alignParentRight="true"
        android:layout_alignWithParentIfMissing="true"      
        android:gravity="center"
        android:textSize="12sp" 
        android:layout_margin="5dp"
        android:layout_alignParentBottom="true"
        android:text="@string/list_item_model"
         />
</RelativeLayout>